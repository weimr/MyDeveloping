package com.weimr.spock;

public class SpockTest {

/*    def "年龄比较，用户1：#age1，用户2：#age3，结果值：#expectVal"() {
        expect:
        SpockDemo.compare(new User(age:age1), new User(age:age2)) == expectVal

        where:
        age1 | age2 || expectVal
        null | null || -1
        10   | null || -1
        null | 10   || -1
        5    | 10   || -1
        10   | 5    || 1
    }*/
}
